/** Полный цикл прогресса 0→100% на экране входа и при выходе из профиля (как AuthLandingPage). */
export const AUTH_POST_LOADER_DURATION_MS = 5800;
/** Сколько держим крупный текст CRM до появления квадрата МАРС. */
export const AUTH_POST_LOADER_CRM_BEFORE_MARS_MS = 2800;
